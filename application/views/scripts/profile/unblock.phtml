<?php $user = $this->user ?>
<?php $this->headTitle()->prepend('Profile for ' . $user->getDisplayName()) ?>

<div id="sidebar">
    <?php echo $this->partial('profile/_sidebar.phtml',
        array('user' => $user, 'menu' => $this->menu)) ?>
</div>

<div id="page">
    <div class="main">
        <h1>Unblock User</h1>
        <p>Un-blocking this user will allow them to log in to Sageweb.</p>

        <form action="<?php echo $this->url() ?>" method="post">
            <input type="submit" value="Unblock User" />
        </form>
    </div>
</div>